# Card Hover Scale Component

A fluid micro-interaction component for the EaseMotion CSS framework that scales a card element up smoothly when focused or hovered over by a cursor.

## Installation / Setup

Place the component files inside your `submissions/` directory:
```bash
submissions/examples/card-hover-scale/
├── demo.html
├── style.css
└── README.md