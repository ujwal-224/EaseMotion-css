# AI City Intelligence Platform

A premium smart city dashboard with traffic flow analyzers, air quality scorecards, and public utility logs using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, pulse animations, hover transitions, and fade-up effects with zero JS dependencies.