# Business Continuity Command Center

A premium enterprise resilience dashboard with disaster recovery drills, backup status panels, and secondary node latency stats using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, animated backup meters, hover transitions, and fade-up effects with zero JS dependencies.