# Autonomous Vehicle Operations Dashboard

A premium enterprise AV fleet dashboard with fleet telemetry trackers, sensor load metrics, and active dispatch logs using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, animated progress meters, hover transitions, and fade-up effects with zero JS dependencies.