# AI-Powered Strategy Center

A premium enterprise strategy dashboard with market indicators, ARR forecast metrics, and workflow logger console using pure CSS.

## Usage
Open `demo.html` directly in a browser — no server needed.

## Why EaseMotion CSS?
Demonstrates premium CSS layout, glow animations, hover transitions, and fade-up log animations with zero JS dependencies.